const span = document.createElement("span")
const text = document.createTextNode("Hello, this is biswash")

// btn.addEventListner('click', () => {
//     console.log('Hello')

const appending = () => {

    span.appendChild(text)
    document.body.appendChild(span)
}  
// },false )


