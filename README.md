```This repo depict the learning traces of mine```

```mermaid
graph TD;
    A(Initial Commit)-->B[/Learning/];
    B --> progress[/Progress/]
    progress --again-->
    progress --recurring process-->
    progress --failed attempts-->
    progress --patience-->
    progress-->E(((Aspiring Developer)))
```